# Copy this file to .env (same folder) and paste your key after the = sign.
# .env is git-ignored, so the key never gets committed.
GEMINI_API_KEY=
# Optional: choose another Gemini model
# PRISM_LLM_MODEL=gemini-2.5-flash
