# Copy this file to .env (same folder) and paste your key after the = sign.
# .env is git-ignored, so the key never gets committed.
GEMINI_API_KEY=
# Optional: Gemini models to try, in order (comma-separated)
# PRISM_LLM_MODEL=gemini-3.8-flash,gemini-3.6-flash,gemini-3.5-flash
